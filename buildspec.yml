version: 0.0
containers:
    LambdaFunctions:
        phases:
            install:
                commands:
                    - apt update
                    - apt install -y npm
                    - npm config set strict-ssl false
                    - npm install -g serverless
            build:
                commands:
                    - serverless package
